public class StringAndIntConcat { // Start of the class
    public static void main(String[] args) { // Main method: program starts here

        String name = "João"; // String variable to store the name
        int age = 25; // Integer variable to store the age

        // Concatenate name and age; Java converts the integer to a string automatically
        System.out.println(name + " is " + age + " years old."); // Print the final message
    }
}
